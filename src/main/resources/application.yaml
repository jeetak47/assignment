server:
  port: 9090
  servlet:
    context-path: /assignement
  #Show all queries
spring:
  jpa:
    show-sql: true
  properties:
    hibernate:
      format_sql: true
logging:
  level:
    org:
      hibernate:
        type: trace